version: '3.8'

services:
  journal-service:
    build: .
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_SERVER=dpg-d0a1571r0fns73e0melg-a.singapore-postgres.render.com:5432
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=pAdo1Zeme66SHatZmtFnpF9CFklo2Ugx
      - POSTGRES_DB=kannectdb
      - JWT_SECRET=2e23d7dc-3ab6-4fcd-b698-ede98aab8539
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - DEBUG=True
      - NLP_MODEL=en_core_web_sm
    networks:
      - kannect-network
    volumes:
      - ./app:/app/app
    restart: unless-stopped

networks:
  kannect-network:
    driver: bridge 